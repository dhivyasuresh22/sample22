# REQUIRES-PROPERTIES-FILE - # Do not remove this comment!
CREATE OR REPLACE DATASOURCE DF ds_a
    FOLDER = '/1 - connectivity'
    ROUTE LOCAL 'LocalConnection' '${databases.denodo_training.folder.1 - connectivity.datasources.df.ds_a.ROUTE.LOCAL.PATH}'
    COLUMNDELIMITER = ','
    ENDOFLINEDELIMITER = '\n'
    HEADER = FALSE;

